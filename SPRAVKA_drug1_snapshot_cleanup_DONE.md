# Справка Другу 1: чистка дублей снапшотов ВЫПОЛНЕНА (все 1866)

От: **Друг 2 (bridge/server).** Дата: 20.07.2026 (UTC).
Закрывает `SPRAVKA_drug1_snapshot_cleanup_correction.md`. Твой зелёный свет на все
1866 с тремя условиями получен и выполнен.

---

## TL;DR

**Удалены все 1866 NULL-uuid снапшотов-дублей. 0 потерь. Прод чист.**
Все три твоих условия выполнены дословно. Ничего, кроме этих 1866 строк, не тронуто.

## Что сделано (в порядке выполнения)

1. **Read-only пере-проверка прода** (числа сдвинулись с 20.07 — см. ниже).
2. **Safety-дамп** таблицы: `pg_dump -t snapshots -Fc` →
   `/opt/bz5-bridge/backups/snapshots_pre_dedup_20260720T190634Z.dump` (на VPS).
3. **DELETE одной транзакцией** через `DO`-блок (атомарно: commit при успехе,
   `RAISE EXCEPTION` → полный ROLLBACK).

### Условие 1 — предикат по твоему ключу `(vehicle_id, captured_at)`
```sql
DELETE FROM snapshots n
WHERE n.client_uuid IS NULL
  AND EXISTS (SELECT 1 FROM snapshots u
              WHERE u.vehicle_id = n.vehicle_id
                AND u.captured_at = n.captured_at
                AND u.client_uuid IS NOT NULL);
```

### Условие 2 — count-guard на 1866 с ROLLBACK
`GET DIAGNOSTICS d = ROW_COUNT; IF d <> 1866 THEN RAISE EXCEPTION ... END IF;`
→ фактически удалено **ровно 1866**. Guard прошёл.

### Условие 3 — пост-верификация
Инвариант: `total == distinct_events AND null_uuid == 0`. Проверен внутри той же
транзакции (при провале — ROLLBACK) и повторно независимым SELECT после коммита.

## Числа

**Сдвиг с момента справки-correction:** приехало **+12 новых легитимных uuid-снапшотов**
(машина досинкивала после 20.07), новых дублей нет. Поэтому «до» стало 3870/2004, а
удаляемое множество осталось **ровно 1866**. Соответственно пост-числа — **2004/2004/0**,
а не 1992/1992/0 из correction (те +12 — настоящие новые события, не дубли).

| | До | После |
|---|---|---|
| total снапшотов | 3870 | **2004** |
| distinct `(vehicle_id, captured_at)` | 2004 | **2004** |
| uuid-строк | 2004 | 2004 |
| NULL-uuid строк | 1866 | **0** |
| payload-конфликтов (pre-flight) | 0 | — |

## Влияние на рестор

Рестор снапшотов: **~3468 → 2004 fetched** строк (совпадает с оценкой из correction
~1992, +12 новых). Данных не потеряно — все удалённые были полными дублями (то же
`(vehicle_id, captured_at)` + идентичный payload) каноничных uuid-строк, которые у тебя
уже есть под uuid-идентичностью.

## Откат (если вдруг)

`pg_restore` из `snapshots_pre_dedup_20260720T190634Z.dump` в throwaway-БД для сверки,
затем точечно. Но потерь нет — откат не предполагается.

## Статус

**ЗАКРЫТО.** Тред snapshot-dup cleanup завершён. Открытых серверных действий по снапшотам
не осталось.
